<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Symulator Bilarda - Etap 3" Height="750" Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Panel kontrolny -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Liczba kul:" VerticalAlignment="Center"/>
            <TextBox Text="{Binding BallCount, UpdateSourceTrigger=PropertyChanged}" 
                     Width="50" Margin="5,0" VerticalAlignment="Center"/>

            <Button Content="Utwórz kule" Command="{Binding CreateBallsCommand}" 
                    Margin="10,0" Padding="10,5"/>
            <Button Content="Start" Command="{Binding StartCommand}" 
                    Margin="10,0" Padding="10,5"/>
            <Button Content="Stop" Command="{Binding StopCommand}" 
                    Margin="10,0" Padding="10,5"/>

            <Separator Width="20"/>

            <Button Content="Diagnostyka ON/OFF" Command="{Binding ToggleDiagnosticsCommand}" 
                    Margin="10,0" Padding="10,5"/>
        </StackPanel>

        <!-- Panel diagnostyczny -->
        <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,5" Padding="5">
            <TextBlock Text="{Binding DiagnosticStatus}" FontSize="10" 
                       Foreground="Black"/>
        </Border>

        <!-- Stół bilardowy -->
        <Border Grid.Row="2" BorderBrush="SaddleBrown" BorderThickness="10" CornerRadius="5">
            <Grid Background="DarkGreen">
                <ItemsControl ItemsSource="{Binding Balls}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas Width="{Binding TableWidth}" Height="{Binding TableHeight}"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Ellipse Width="{Binding Diameter}" 
                                     Height="{Binding Diameter}"
                                     Fill="Red" 
                                     Stroke="DarkRed" 
                                     StrokeThickness="2"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding X}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                </ItemsControl>
            </Grid>
        </Border>
    </Grid>
</Window>